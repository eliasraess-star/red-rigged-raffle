# red-rigged-raffle

Clean Code Exercise

## Usage

Run the main program:

    deno run main.ts

Run the unit tests:

    deno test

Report test coverage (see `coverage/html/index.html`):

    deno test --coverage
